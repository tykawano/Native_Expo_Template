{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "types": ["nativewind/types"], // Add NativeWind types
    "jsx": "react-native" // Ensure proper JSX support for React Native
  }
}
